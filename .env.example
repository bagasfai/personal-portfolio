# Web3Forms access key — https://web3forms.com (public by design, safe in the bundle)
# Copy this file to .env.local and paste your key. Without it the form takes the
# error path rather than silently pretending to have sent something.
NEXT_PUBLIC_CONTACT_ACCESS_KEY=
